# Shared secret. The Shortcut sends it as X-API-Key; browser views accept ?k=<key>.
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
REELNOTES_API_KEY=

ANTHROPIC_API_KEY=

# Where notes live. On Fly/Railway point this at a mounted volume so a redeploy
# does not wipe your notes.
REELNOTES_DB=/data/reelnotes.sqlite3

# Push. Pick a long random topic name — anyone who knows it can read your notes.
NTFY_TOPIC=reelnotes-CHANGE-THIS-a8f2c1
NTFY_SERVER=https://ntfy.sh

# Used to make the notification tappable. Set to your deployed URL.
PUBLIC_BASE_URL=

# Most Instagram/Facebook URLs need a logged-in session. Export cookies from a
# browser where you are logged in, in Netscape format, and mount the file here.
YTDLP_COOKIES_FILE=/data/cookies.txt

# faster-whisper (local, free, slower) or openai (hosted, ~$0.006/min).
REELNOTES_ASR=faster-whisper
REELNOTES_WHISPER_MODEL=small
REELNOTES_MODEL=claude-opus-5
REELNOTES_FRAMES=4
REELNOTES_MAX_DURATION=900
